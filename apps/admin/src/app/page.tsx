import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Dashboard",
  description: "Admin dashboard for managing content and system settings.",
};
export default function DashboardPage() {
  return (
    <div>Dashboard Content</div>
  )
}

